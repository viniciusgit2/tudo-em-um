export default function Titulo() {
  return (
    <div className="bg-gradient-to-br bg-amber-300">
      {" "}
      <legend className="text-teal-400 rounded-br-lg justify-center bg-black">
        titulo:cadastro
      </legend>
      <legend className="text-teal-400 rounded-br-lg justify-center bg-black">
        subtitulo:simples quase completo
      </legend>
      <legend className="text-teal-400 rounded-br-lg justify-center bg-black">
        conteudo:tanto faz
      </legend>
      <footer className="text-teal-400 rounded-br-l">
        vamos ver se fica embaixo?+-
      </footer>
    </div>
  );
}
